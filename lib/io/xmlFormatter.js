"use strict";

var OutputFormatter = require("./OutputFormatter"),
    xmlFormatter;

function formatter(data) {
  //passar les dades a xml
  return data;
}

xmlFormatter = new OutputFormatter(formatter);

jsonFormatter.name = "xmlFormatter";

module.exports = xmlFormatter;